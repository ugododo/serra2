---
// Individual Artist Detail Page
// Create one file for each artist in: src/pages/artists/[artist-slug].astro
// Example: src/pages/artists/artist-name-1.astro

// Import components
import '../../styles/global.css';
import Navigation from '../../components/navbar.astro';

// Base URL for GitHub Pages
const baseUrl = '/serra2';

// Artist data - Update this for each artist
const artist = {
  name: "Michele Braschi",
  photo: `${baseUrl}/artists-artworks/Braschi.jpg`,
  bio: "Michele Braschi is a professional Italian illustrator who has dedicated himself to altering Magic: The Gathering cards for many years. Like the other artists in this exhibition, he brings a unique style to his illustrations, creating them from start to finish, from the initial pencil sketch to the final acrylic painting. He particularly favors small formats, which allow him to give free rein to his imagination and refine the details endlessly… even if his eye sometimes dictates otherwise. A card altered by Michele is immediately recognizable by the Universe that emerges from it, different from the MTG Universe, between poetry and cartoons, sometimes ferocious. Contact: Mibalters on Instagram ",
  artworks: [
    {
      title: "Artwork Title 1",
      image: `${baseUrl}/artists-artworks/BraschiCard.jpg`,
      description: "A detailed description of this artwork, the concept behind it, and what makes it special."
    },
    {
      title: "Artwork Title 2",
      image: `${baseUrl}/artists-artworks/BraschiFramed.jpg`,
      description: "A detailed description of this artwork, the concept behind it, and what makes it special."
    },
    {
      title: "Artwork Title 3",
      image: "/artworks/artist1-work3.jpg",
      description: "A detailed description of this artwork, the concept behind it, and what makes it special."
    }
  ]
};

// Page metadata
const pageTitle = `${artist.name} - Serra 2.0`;
const pageDescription = `Explore ${artist.name}'s artworks for the Serra 2.0 project`;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content={pageDescription}>
  <title>{pageTitle}</title>
</head>

<body>
  <!-- Import Navigation Component -->
  <Navigation currentPage="artists" />

  <!-- Artist Header with Photo -->
  <header class="artist-detail-header">
    <div class="container">
      <a href={`${baseUrl}/artists`} class="back-link">← Back to Artists</a>
      <div class="artist-header-content">
        <div class="artist-header-text">
          <h1 class="artist-detail-name">{artist.name}</h1>
          <p class="artist-detail-bio">{artist.bio}</p>
        </div>
        <div class="artist-header-photo">
          <img src={artist.photo} alt={artist.name} />
        </div>
      </div>
    </div>
  </header>

  <!-- Artworks Section -->
  <main class="container">
    <h2 class="section-title">Artworks</h2>
    <div class="artist-detail-artworks-grid">
      {artist.artworks.map((artwork) => (
        <div class="artist-detail-artwork-card">
          <div class="artist-detail-artwork-image">
            <img src={artwork.image} alt={artwork.title} />
          </div>
          <div class="artist-detail-artwork-info">
            <h3 class="artist-detail-artwork-title">{artwork.title}</h3>
            <p class="artist-detail-artwork-description">{artwork.description}</p>
          </div>
        </div>
      ))}
    </div>
  </main>

  <!-- Footer -->
  <footer>
    <p>&copy; 2026 Serra 2.0 Project. All rights reserved.</p>
  </footer>
</body>
</html>